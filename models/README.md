# Model storage

No third-party checkpoint is redistributed. Register ONNX or safetensors models through the API and mount checkpoints into this directory in controlled deployments. The optional YOLO adapter downloads or loads a checkpoint only when explicitly configured.
