"use client";import {API_URL,getToken} from "@/lib/api";
export default function Reports(){async function download(){const response=await fetch(`${API_URL}/api/v1/reports/alerts.csv`,{headers:{Authorization:`Bearer ${getToken()??""}`}});if(!response.ok)throw new Error("Report export failed");const blob=await response.blob();const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="aeroramp-alerts.csv";a.click();URL.revokeObjectURL(url)}return <div className="grid two-col"><section className="panel"><h2>Ramp-safety alert export</h2><p className="muted">Generates a CSV from tenant-scoped database records. Export activity is permission-controlled.</p><button className="primary-button" onClick={download}>Download alert CSV</button></section><section className="panel"><h2>Available report domains</h2><p>Turnaround performance</p><p>Safety alerts and resolution</p><p>Stand utilization</p><p>Camera and processing health</p><p className="muted">The included API provides the alert CSV implementation and a documented extension pattern for additional reports.</p></section></div>}
